# 水卢冷门高报引擎 · 产品体验改进意见书

> 评审角色：资深产品工程师  
> 评审范围：Web 前端全链路（首页 → 查询 → 结果 → 支付 → 个人中心 → 工具页）  
> 评审日期：2026-06-16  
> 优先级：🔴 高 | 🟡 中 | 🟢 低

---

## 一、全局 / 导航层

### 1. 🔴 导航栏信息密度过载
**现状**：Nav 同时承载品牌名、副标题（袁希团队出品）、5 个入口链接（学校库/对比/专业风向标/志愿表/AI 助手）、AuthNav（登录/用户菜单）、移动端汉堡菜单。  
**问题**：在 680px 容器宽度下，桌面端链接已接近换行临界点；移动端汉堡菜单与 AuthNav 同时存在，顶部右侧拥挤。  
**建议**：
- 将「学校库」「对比」收入「工具」下拉，保留核心高频入口（志愿表、AI 助手）
- 或采用 Apple 式「更多」收纳：`学校库 · 对比 · 专业风向标` 合并为「探索 ▸」
- 副标题「袁希团队出品」在滚动后应隐藏（`apple-nav` 已有 sticky，可配合滚动状态切换）

### 2. 🔴 缺少全局加载/网络异常状态
**现状**：除 dashboard 有一个 spinner 外，绝大多数页面（结果页、学校详情、搜索）在弱网或 API 500 时直接白屏或报错。  
**建议**：
- 全局接入 `apiFetch.ts` 中的统一错误拦截，增加「网络开小差」占位图 + 重试按钮
- 结果页已加 retry，但学校详情页、搜索页、对比页均未加

### 3. 🟡 缺少页面级面包屑
**现状**：用户从结果页 → 学校详情 → 返回时，很容易丢失上下文（尤其是新开标签页直接进学校详情）。  
**建议**：学校详情页顶部增加面包屑：`北京 · 位次 28000 · 物理+化学+生物 > 北京大学`

### 4. 🟡 反馈入口单一且隐蔽
**现状**：FeedbackModal 仅通过点击「袁希团队出品」触发，这是一个极低 discoverability 的入口。  
**建议**：在 FloatingService 或全局右下角增加「反馈」气泡图标（非侵入式），与客服微信并列。

---

## 二、首页 / 查询表单（Landing）

### 5. 🔴 Hero 文案与转化漏斗脱节
**现状**：H1「有人用你的分数进了比你好20名的大学」很棒，但 CTA 按钮下方缺少社会信任状（如「已有 X 万考生使用」实时滚动）。  
**建议**：在 Data Strip 增加一条实时感数据：`「刚刚有河北考生通过这里发现 3 所隐藏好校」`（可前端随机轮播，不依赖后端）。

### 6. 🔴 选科错误提示不够醒目
**现状**：3+1+2 模式下，首选未选或再选不足时，仅有一行 11px 红色文字「请再选 2 科（已选 1 科）」。  
**问题**：用户可能已滚动到「查询」按钮处，看不到上方的错误提示。  
**建议**：
- 错误时自动 scroll to first error
- 提交按钮 disabled 状态给出明确原因文案（如「请完成选科后再查询」）
- 或在选科区域顶部增加一个微型状态条（类似进度指示）：`选科完成度 2/3`

### 7. 🟡 偏好约束折叠后不可见，用户易遗忘
**现状**：偏好约束（城市/层次/专业/批次/性别/专项）折叠后，用户已填内容没有任何提示。  
**建议**：在「展开偏好约束」按钮右侧增加一个小徽标摘要，如「已选：北京/公办/本科」，让用户感知约束已生效。

### 8. 🟡 省份数据状态提示过于技术化
**现状**：`数据完整（2017–2025，9年）✓` 对普通用户无意义。  
**建议**：改为用户价值导向的文案：`覆盖 9 年真实录取数据，含 2025 最新` 或 `数据更新至 2025，预估更准确`。

### 9. 🟢 查询历史（HistoryQuickAccess）缺少删除操作
**现状**：最近查询只展示，无法删除，用户隐私敏感时会产生焦虑。  
**建议**：每条历史右侧增加 `×` 删除，或提供「清空历史」入口。

---

## 三、结果页（Results）

### 10. 🔴 结果页顶部 Tab 与付费墙信息层级混乱
**现状**：结果页顶部有「冲/稳/保/冷门宝藏」Tab，下面紧跟「您已解锁 X 所 / 剩余 Y 所锁定」banner。  
**问题**：新用户首次进入时，注意力被同时拉扯到「分类」和「付费」，不知道先看哪个。  
**建议**：
- 将解锁 banner 改为更轻量的胶囊提示（如 `已解锁 15/96 所 · 查看完整`），降低视觉权重
- 或把 banner 做成可 dismiss 的（记住 dismiss 状态到 sessionStorage）

### 11. 🔴 锁定卡片（LockedSchoolCard）的 CTA 过弱
**现状**：锁定卡只有学校名、城市、 tier 标签和一个模糊按钮。  
**问题**：用户无法从锁定卡获得任何「为什么要解锁」的动机。  
**建议**：在锁定卡中强制透出 1-2 条高价值 preview（如就业率、城市级别），并文案引导：「解锁查看该校为何被 2,847 人关注」。

### 12. 🔴 就业数据「解锁」与「查看」的交互断层
**现状**：未付费时就业数据区块显示模糊 + 🔒 解锁查看就业数据。点击后仅触发 `onUnlock`，但用户可能不知道解锁后就业数据会出现在哪里。  
**建议**：
- 模糊区块点击后应直接唤起 PayModal（当前似乎依赖父组件传递 onUnlock，但体验不够直接）
- 解锁后就业数据应有一个高亮入场动画（如 fade-in + unblur），让用户感知变化

### 13. 🟡 学校卡片内的「对比分析」按钮在已选 3 所后无反馈
**现状**：对比最多 3 所，第 4 次点击时 showToast「最多对比3所学校」。  
**建议**：当已选满 3 所时，卡片上的「对比分析」按钮应自动变为 disabled 状态并文案改为「对比已满」，避免无效点击。

### 14. 🟡 概率条与建议行动（suggested_action）的视觉关系弱
**现状**：概率条在卡片左侧，suggested_action 在卡片底部，两者物理距离远。  
**建议**：将 suggested_action 融入概率条右侧，形成「概率 + 建议」的紧凑信息块，减少眼球移动。

### 15. 🟡 大小年提示的可见性不足
**现状**：`大小年：2025 偏冷` 仅在展开时显示，且文案对普通用户理解门槛高。  
**建议**：
- 在卡片顶部 Badge 区增加一个微型状态（如 `🧊 偏冷` / `🔥 偏热`），让用户一瞥即知
- 或在「近年均位次」旁增加箭头图标（↑↓→）表示趋势

### 16. 🟢 缺少「分享这个结果」能力
**现状**：考生家长常需要把结果发给家人讨论。  
**建议**：在结果页顶部增加「分享」按钮，生成带 query 参数的短链接（或截图指引），并附带一句话：「复制链接给家人，他们无需登录即可查看」。

---

## 四、学校详情页（School Detail）

### 17. 🔴 返回逻辑缺失上下文
**现状**：学校详情页顶部只有「← 返回」，但返回后不会带回用户的查询条件（ province/rank/subject ）。  
**建议**：从结果页进入学校详情时，强制将查询参数写入 URL，返回按钮使用 `router.back()` 或 `history.go(-1)`，已在 URL 中保留上下文。但更好的做法是面包屑导航（见 #3）。

### 18. 🔴 专业列表缺少搜索/筛选
**现状**：热门学校（如郑州大学）可能有 50+ 专业，全部平铺，用户难以快速定位。  
**建议**：在专业列表顶部增加「按专业名搜索」和「按选科过滤」两个输入框。

### 19. 🟡 学科评估颜色过于刺眼
**现状**：A+ 用 `#ff3b30`（纯红），在浅色背景下非常刺眼。  
**建议**：降低饱和度，使用品牌色系内的红色变体（如 `#DC2626` → `#EF4444`），或统一使用 Navy/Accent 体系。

### 20. 🟢 缺少「该校历年分数线」总览图
**现状**：每个专业折叠后展示历年数据，但用户很难横向对比不同专业。  
**建议**：在页面顶部增加一个全校「位次区间分布图」（如箱线图），展示该校各专业的录取位次跨度。

---

## 五、定价页（Pricing）

### 21. 🔴 定价页是纯展示，无法直接购买
**现状**：三个方案的 CTA 都是纯 `div`（非按钮），不可点击，文案为「¥39 解锁单次」。  
**问题**：用户从结果页跳转到 `/pricing` 后，无法直接下单，需要再回结果页点击锁定卡。这违反了「每步都可成交」的电商原则。  
**建议**：
- 定价页 CTA 直接唤起 PayModal，传入对应 product_type
- 或 PricingPage 直接移除，所有支付在结果页或 dashboard 完成（减少一个漏斗层级）

### 22. 🟡 缺少价格锚定和紧迫感
**现状**：季会员 ¥99 与单次 ¥39 的对比不够强烈。  
**建议**：在单次方案下方增加小字：`「如果你计划查 3 次以上，季会员更划算」`，或在季会员卡上增加「🔥 78% 用户的选择」标签。

### 23. 🟢 年度/月度切换无意义
**现状**：代码中有 `const [annual, setAnnual] = useState(false)`，但 UI 没有展示年付切换。  
**建议**：若暂无年付计划，应删除此 state，避免代码债务。

---

## 六、个人中心（Dashboard）

### 24. 🟡 会员状态展示在过期当天会产生信任危机
**现状**：`days_remaining` 在服务端计算，前端直接展示。如果用户过期日当天早上看到「剩余 0 天」或「已到期」，可能产生「我刚付的钱」错觉。  
**建议**：增加缓冲文案：「会员将于今日 23:59 到期」比「❌ 会员已到期」更柔和，且应在到期后保留 24h 的「宽限期展示」。

### 25. 🟡 已购报告列表缺少「重新查询」操作
**现状**：已购报告只有「查看报告」链接。  
**建议**：增加「用相同条件再查一次」按钮，因为用户可能想基于历史条件微调后重新查询（这是季会员的核心价值）。

### 26. 🟡 邀请好友文案缺少直接分享按钮
**现状**：邀请模块需要用户点击「复制专属邀请链接」，然后手动打开微信粘贴。  
**建议**：增加「直接分享到微信」按钮（调用微信 JS-SDK 或生成图文卡片），降低分享摩擦。

### 27. 🟢 提现成功后的「截图」提示太原始
**现状**：提现成功弹窗要求用户「截图此页面，添加客服微信」。  
**建议**：
- 直接显示客服微信二维码图片（减少用户手动搜索）
- 或自动生成带订单号的截图预览（HTML2Canvas）

---

## 七、志愿表（Form）

### 28. 🟡 缺少「一键导出」功能
**现状**：志愿表只能在线查看和排序，无法导出为 PDF/图片/Excel。  
**建议**：在志愿表顶部增加「导出志愿表」按钮，生成适合打印的 A4 排版（或长图分享），这是高报场景的高频需求。

### 29. 🟡 拖拽排序在移动端体验差
**现状**：使用 HTML5 drag & drop，在 iOS Safari 上支持不佳且容易误触。  
**建议**：在移动端改用「长按 + 上下箭头」或「长按进入编辑模式」的交互。

### 30. 🟢 冲/稳/保/极冲 的统计缺少比例建议
**现状**：顶部显示各分类数量，但没有给出「黄金比例」建议（如 2:3:3:1）。  
**建议**：增加一个微型建议文案：「建议冲:稳:保 = 2:4:4，当前比例偏保守」。

---

## 八、学校对比（Compare）

### 31. 🟡 对比页缺少「添加学校」的快捷入口
**现状**：对比页依赖从结果页/学校详情页「加入对比」，若用户直接进入 `/compare` 为空状态。  
**建议**：空状态时展示「搜索学校加入对比」输入框，而非单纯的空白。

### 32. 🟢 对比维度缺少「就业薪资」和「专业重合度」
**现状**：对比维度集中在学校属性（排名、保研率、男女比例），缺少决策相关维度。  
**建议**：增加「该考生可报专业交集」对比（如考生选科+位次下，两校各自能报哪些专业）。

---

## 九、搜索页（Search）

### 33. 🟡 按专业找学校时，位次输入体验不佳
**现状**：用户需要先输入专业关键词，再输入位次，再选省份，才能搜索。  
**建议**：如果用户从结果页跳转过来，应自动带入当前查询的 province/rank/subject，减少重复输入。

### 34. 🟢 搜索结果缺少「快速加入志愿表」
**现状**：搜索到学校后，需要点进学校详情才能加入志愿表。  
**建议**：在搜索结果卡片上直接增加「+ 志愿表」按钮。

---

## 十、专业风向标（Major Trend）

### 35. 🟡 内容页过长，缺少目录/锚点导航
**现状**：专业风向标是一个超长内容页（专业危机分析、分类标签、宏观建议、微观建议），用户滚动时容易迷失。  
**建议**：左侧/顶部增加 sticky 目录（如 `产业结构 · AI替代 · 出生人口 · 政策导向 · 就业寒冬`），允许点击跳转。

### 36. 🟡 数据可视化不足，纯文本说服力弱
**现状**：所有数据以文字和卡片形式呈现，没有图表。  
**建议**：将「撤销专业数量」「毕业人数」「出生人口」等数据做成小型折线图/柱状图，增强视觉冲击力。

---

## 十一、管理后台（Admin）

### 37. 🔴 后台缺少数据导出
**现状**：所有数据（订单、用户、事件）只能在线查看，无法导出 CSV/Excel。  
**建议**：在每个表格顶部增加「导出当前筛选」按钮，支持 CSV 下载（后端已有数据，只需加 `text/csv` 接口）。

### 38. 🟡 事件表格缺少批量操作
**现状**：事件列表只能逐条查看。  
**建议**：增加按 event_type 快速筛选的 tab（如 `全部 · 查询 · 点击 · 支付`），减少管理员翻页成本。

### 39. 🟢 热门学校 Top15 的「点击数」缺少时间维度
**现状**：Top15 展示的是全部历史点击数，无法看出「今天哪所学校突然热了」。  
**建议**：增加「今日点击增量」排序，或给一个切换开关（累计 / 今日）。

---

## 十二、支付体验（PayModal）

### 40. 🔴 微信内 JSAPI 失败后的 fallback 不够明显
**现状**：JSAPI 失败后调用 `fallbackToNative()`，但用户端没有明确感知到「已切换支付方式」。  
**建议**：fallback 时应在弹窗内展示一个明确的状态变更 toast：`「微信支付唤起失败，已切换为扫码支付」`。

### 41. 🟡 模拟支付模式（simulateMode）仅在 localhost 开启，但无入口提示
**现状**：开发者知道在 localhost 可以模拟支付，但测试/运营人员不知道。  
**建议**：在 localhost 环境下，在 PayModal 顶部展示一个醒目的绿色横幅：`「当前为模拟支付环境，不会扣款」`。

### 42. 🟡 二维码倒计时文案过于技术化
**现状**：显示 `7200` 秒倒计时。  
**建议**：转换为「剩余 2 小时」或「剩余 119 分 59 秒」，更自然。

---

## 十三、总结与优先级路线图

### 第一阶段（立即做，转化提升最明显）
1. 定价页 CTA 直接唤起支付（#21）
2. 结果页锁定卡增加 preview 动机（#11）
3. 首页选科错误提示自动滚动 + 提交按钮 disabled 原因（#6）
4. 全局弱网/错误状态页（#2）
5. 学校详情页返回保留上下文（#17）

### 第二阶段（本周做，体验打磨）
6. 结果页解锁 banner 轻量化（#10）
7. 志愿表增加导出功能（#28）
8. 偏好约束折叠后显示摘要（#7）
9. 对比页空状态优化（#31）
10. 后台数据导出（#37）

### 第三阶段（未来迭代）
11. 专业风向标图表化（#36）
12. 结果页分享能力（#16）
13. 对比维度增加专业重合度（#32）
14. 学校详情专业搜索（#18）

---

> 本意见书基于 2026-06-16 的代码快照撰写。如有特定模块需要进一步拆解交互原型或竞品对标，可继续深入。
