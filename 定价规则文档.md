# 高报引擎 — 前端定价规则文档

> 本文档供小程序端复制定价逻辑使用。所有金额单位：前端展示用"元"，后端/支付接口用"分"。

---

## 一、产品定义（3 个在售产品）

| productType | 展示价 | 后端价（分） | 产品名称 | 核心权益 |
|-------------|--------|--------------|----------|----------|
| `trial_report` | ¥9.9 | 990 | 试看报告 | 每类院校解锁前 3 所（冲/稳/保/冷门宝藏 各 3 所），含录取概率、就业薪资 |
| `single_report` | ¥39 | 3900 | 单次完整报告 | 本次查询的全部院校完整分析（全部 96 所深度分析、安全线、风险提醒、近 3 年趋势、在读生口碑、PDF 下载）。**本次查询永久解锁**，但不支持无限次重新查询 |
| `season_2026` | ¥99 | 9900 | 2026 填报季会员 | 包含单次完整报告全部内容 + **期内无限次重新查询** + 位次微调随时重查 + 志愿表收藏 + 院校对比 + PDF 随时下载。有效期至 **2026-09-01 23:59:59** |

### 向后兼容的旧产品（不再对外销售，但订单仍有效）
- `report_export` — ¥5.9（旧版单次报告）
- `monthly_sub` — ¥19.9（月度会员，+30 天）
- `quarterly_sub` — ¥29.9（季度会员，+90 天）

---

## 二、前端选项过滤规则（PayModal / 支付弹窗）

根据用户已购产品类型 `existingProductType`，动态过滤可选项：

| 用户已购 | 过滤后可见选项 | 默认选中 |
|----------|----------------|----------|
| 未购买任何产品 | ¥9.9 / ¥39 / ¥99 | ¥39 |
| `trial_report`（试看） | ¥39 / ¥99 | ¥39 |
| `single_report` 或 `report_export`（单次） | 仅 ¥99 | ¥99 |
| `season_2026`（季会员） | 全部隐藏（无需再付费） | — |

> 前端过滤数组 `visibleProducts` 逻辑：
> - 已购 trial → 隐藏 trial
> - 已购 single/report_export → 只保留 season_2026

---

## 三、结果页解锁按钮策略

### 未付费用户（`!data.is_paid && !data.is_trial`）
- 第 3 所锁定卡片按钮："¥9.9 解锁前三所" → 打开 PayModal，预选中 `trial_report`
- 第 4 所及以后锁定卡片按钮："¥39 解锁完整报告" → 打开 PayModal，预选中 `single_report`
- 底部固定横幅："¥9.9 起解锁" → 打开 PayModal，不预设类型

### 试看用户（`data.is_trial`）
- 所有锁定卡片按钮："¥39 解锁完整报告" → 打开 PayModal，预选中 `single_report`
- 底部固定横幅："升级完整报告 ¥39" → 同上

### 季会员推广卡片
- 未付费或试看用户，在结果页展示 ¥99 季会员推广卡片
- 点击后打开 PayModal，预选中 `season_2026`

---

## 四、后端订阅到期设置

订单支付成功后，后端 `payment.py` 的 `_mark_paid` 中设置用户订阅状态：

| productType | is_paid | subscription_type | subscription_end_at |
|-------------|---------|-------------------|---------------------|
| `trial_report` | 1 | trial_report | 不变（无到期日） |
| `single_report` | 1 | single_report | 不变（无到期日） |
| `report_export` | 1 | report_export | 不变（无到期日） |
| `season_2026` | 1 | season_2026 | 2026-09-01 23:59:59 |
| `monthly_sub` | 1 | monthly_sub | 当前时间 + 30 天 |
| `quarterly_sub` | 1 | quarterly_sub | 当前时间 + 90 天 |

> **关键**：trial / single / report_export 是永久权益，只看 `is_paid=1`，不检查 `subscription_end_at`。

---

## 五、付费验证三层链路

后端 `main.py` 的 `/api/recommend` 推荐端点中，按以下顺序验证：

1. **Layer 1 — 订单级匹配**（优先级最高）
   - 如果前端传了 `order_no`，检查订单状态 `paid` + 省份/位次/选科匹配
   - 匹配成功 → `is_paid = True`
   - 订阅制订单额外检查用户当前订阅状态是否仍有效（防撤销后仍能用）

2. **Layer 2 — 订阅过期检查**
   - 前端 `/api/auth/me` 中 lazy 检查 `subscription_end_at`
   - 若已过期 → 返回 `is_paid=False`，前端不再传 `order_no`

3. **Layer 3 — 支付失败 UI**
   - PayModal 内处理二维码过期、创建订单失败等状态

---

## 六、邀请返佣规则

- **基础奖励**：每邀请 1 人付费，推荐人会员时长 **+3 天**
- **里程碑奖励**：累计邀请 **4 人** 时，额外一次性 **+30 天**
- 奖励上限：不超过 `2026-09-01`
- 推荐人原本未付费时，自动开通 `is_paid=1` + `subscription_type="season_2026"`

---

## 七、后台管理定价

- **开通会员**：管理员点击"开通" → 后端 `grant_paid` 设置 `subscription_type="season_2026"`，`subscription_end_at=2026-09-01 23:59:59`
- **撤销会员**：后端 `revoke_paid` 清除 `is_paid=0`、`subscription_type=""`、`subscription_end_at=null`

---

## 八、小程序端对接注意

1. **创建订单接口**：`POST /api/payment/create-jsapi`（小程序）或 `POST /api/payment/create-jsapi-web`（公众号）
   - 参数：`openid`、`product_type`、`province`、`rank_input`、`subject`、`c_major`、`c_city`、`c_nature`、`c_tier`
   - 后端根据 `product_type` 自动取 `PRODUCT_AMOUNTS` 中的价格

2. **调起支付**：后端返回 `pay_params`，小程序端调用 `wx.requestPayment(pay_params)`

3. **支付回调**：后端接收微信通知后自动标记订单 `paid`，并发放对应权益

4. **价格硬编码**：后端 `PRODUCT_AMOUNTS` 是价格唯一来源，前端只负责展示，不要在前端硬编码金额做支付校验
